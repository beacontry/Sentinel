// Current version of the Terms of Service + Risk Disclosure. Bump this
// string whenever the language changes materially — users who accepted
// an older version will be re-prompted on their next dashboard load.
//
// Use a date-stamp format ("YYYY-MM-DD") rather than incrementing
// semver — easier to read in audit dumps + matches what the page
// shows in its "Last updated:" line.

export const TERMS_VERSION = "2026-05-17b";
